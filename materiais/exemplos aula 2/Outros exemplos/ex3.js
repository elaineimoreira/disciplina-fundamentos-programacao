let contador = 1;  // Inicializa a variável contador com 1

do {
    console.log(`Contador está em ${contador}`);  // Imprime o valor atual de contador
    contador++;  // Incrementa contador em 1
} while (contador <= 5);  // Continua o loop enquanto contador for menor ou igual a 5
